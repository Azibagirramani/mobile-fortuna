import { View, Text } from "react-native";
import { LogoSheet } from "./style";
const Logo = () => {
  return (
    <View style={LogoSheet.container}>
      <Text style={LogoSheet.text}>F</Text>
    </View>
  );
};

export default Logo;
